import React from "react";
import { useState } from "react";

export default function curvedButton(){
    return(<button
    onClick={()=>{
        //state 변경 
    }}>
         </button>
    

    );
}